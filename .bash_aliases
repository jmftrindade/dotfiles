#!/bin/bash

alias ls='ls -G'
alias ll='ls -la'

# git aliases
alias gb='git branch -a'
alias gl='git log --oneline'

# jfon: make vim point to MacVim's version packaged with maximum-awesome.
alias vim='mvim -v'
